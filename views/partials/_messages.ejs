<div class="row">
  <div class="col">
    <% if (Object.keys(locals.messages).length !== 0) { %>
      <div class="notification is-primary">
        <% for(const type in messages) { %>
          <div>
            <h5><%= messages[type] %></h5>
          </div>
          <% } %>
      </div>
      <% } %>
  </div>
</div>
